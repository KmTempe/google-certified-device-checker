# Base URL for the FastAPI backend. During local development point this to your
# running server (e.g. http://127.0.0.1:8000). Override in CI/CD for production.
VITE_API_BASE_URL=http://127.0.0.1:8000

# Base path for the built assets. Keep '/' locally, but set to '/<repo>/' when
# deploying to GitHub Pages (see pages workflow for example).
VITE_BASE_PATH=/
