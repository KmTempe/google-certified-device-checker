# Base URL for the FastAPI backend. During local development point this to your
# running server (e.g. http://127.0.0.1:8000). Leave empty when the UI is served
# by the FastAPI app itself.
VITE_API_BASE_URL=http://127.0.0.1:8000
